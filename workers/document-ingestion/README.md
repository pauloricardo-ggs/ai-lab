# document-ingestion (descontinuado)

Este worker nao faz parte do runtime nem do roadmap ativo. Upload, extracao, chunks, embeddings, permissoes e Knowledge Bases documentais pertencem exclusivamente ao Open WebUI. O Docling atua como extrator stateless configurado pelo proprio Open WebUI.

O diretorio permanece apenas como registro historico. Nao implemente sincronizacao ou escrita em tabelas tecnicas sem uma nova decisao arquitetural explicita.
