# embedding-worker (descontinuado)

Este worker nao faz parte do runtime atual. O Open WebUI gera e controla embeddings documentais. Na indexacao tecnica, a Admin UI chama o Ollama diretamente e grava os vetores de codigo na collection `code_symbols`.

O diretorio permanece apenas como registro historico e nao deve receber responsabilidades documentais.
